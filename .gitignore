node_modules/
dist/
.next/
.mercato/
generated/
.turbo/
*.tsbuildinfo
*.log
.env
.env.*
.DS_Store

# Jest coverage output. Anchored on purpose: an unanchored `coverage/` would also
# swallow the module's own `api/coverage/` and `backend/pricing/coverage/` sources.
/coverage/
packages/*/coverage/
